# ─── Web framework ───────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# ─── Structured logging ──────────────────────────────────────────────────────
structlog==24.4.0

# ─── SAST engine ─────────────────────────────────────────────────────────────
semgrep==1.97.0
# setuptools provides pkg_resources, which semgrep's opentelemetry-instrumentation
# dependency imports at runtime. Python 3.12 no longer bundles setuptools, so
# without this semgrep crashes on startup (ModuleNotFoundError: pkg_resources).
setuptools==75.6.0

# ─── Quality analysis ────────────────────────────────────────────────────────
radon==6.0.1
lizard==1.17.13
# Pygments lexers drive token-normalized duplicate-code detection (rename- and
# whitespace-insensitive). Pure Python — no C toolchain needed in the slim image.
pygments==2.18.0

# ─── Enrichment ──────────────────────────────────────────────────────────────
# PyYAML loads the context-rich finding templates (enrichment/rule_templates.yaml).
pyyaml==6.0.2

# ─── Local ML (privacy-safe false-positive filter) ───────────────────────────
# Trains/scores on finding METADATA only — never source code. lightgbm needs
# libgomp1 (added in the Dockerfile); scikit-learn provides CV metrics + joblib.
lightgbm==4.5.0
scikit-learn==1.5.2

# NOTE: trivy and gitleaks are installed as binaries in the Dockerfile, not pip.

# ─── Tests ───────────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
