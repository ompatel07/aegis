# Pinned to versions with well-known published CVEs so Trivy SCA has something
# to report. (Fixture only — never use these versions for real.)
flask==0.12.0
jinja2==2.10
pyyaml==5.1
requests==2.19.0
